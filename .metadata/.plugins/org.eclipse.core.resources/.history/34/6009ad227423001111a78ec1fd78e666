/*
 * pwm.c
 *
 *  Created on: Mar 16, 2026
 *      Author: LeeJooHo
 */


#include <pwm.h>

void PWM_Init(PWM* this, TIM_TypeDef* tim, uint32_t ccrMin, uint32_t ccrMax)
{
	this->tim    = tim;
	this->ccrMin = ccrMin;
	this->ccrMax = ccrMax;
}
void PWM_SetAngle(PWM* this, uint8_t angle)
{
    if (angle > 180) angle = 180;

    this->tim->CCR2 = this->ccrMin + ((this->ccrMax - this->ccrMin) * angle) / 180;
}

void PWM_SetCcr(PWM* this, uint32_t ccr)
{
	this->tim->CCR2 = ccr;
}
